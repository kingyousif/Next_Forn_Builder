import { Suspense } from "react";

// import { DataTable } from "@/components/data-table/data-table";

const page = () => {
  return <Suspense>{/* <DataTable columns={columns} /> */}</Suspense>;
};

export default page;
